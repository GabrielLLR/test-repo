name: Validate PR and Create Jira Task

on:
  repository_dispatch:
    types: [trigger_main_workflow]

jobs:
  validate-and-jira:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout do Código do Repositório Oficial
        uses: actions/checkout@v4
        with:
          ref: main  # Sempre pega a versão mais recente da main do repositório oficial
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Obter Código do PR Correto
        run: |
          git fetch origin pull/${{ github.event.client_payload.pr_branch }}/head:pr_branch
          git checkout pr_branch

      - name: Verificar a Branch Atual (Confirma PR)
        run: git branch --show-current

      - name: Debug - Verificar Alterações no PR
        run: git diff --name-only HEAD^ HEAD

      - name: Debug - Listar Arquivos no Diretório (confirma versão do PR)
        run: ls -R

      - name: Debug - Exibir Caminho Completo do `swagger.yaml`
        run: |
          echo "Listando arquivos no diretório atual:"
          ls -lah
          echo "Procurando por swagger.yaml:"
          find $(pwd) -name "swagger.yaml"

      - name: Garantir Permissões de Leitura no `swagger.yaml`
        run: chmod +r swagger.yaml || true

      - name: Criar `validate.go` na raiz com logs detalhados
        run: |
          echo 'package main

          import (
              "fmt"
              "os"
              "github.com/pb33f/libopenapi"
          )

          func main() {
              fmt.Println("🟢 Iniciando validação OpenAPI... GABRIEL LINCOLN")

              if len(os.Args) < 2 {
                  fmt.Println("❌ Erro: Arquivo não fornecido")
                  os.Exit(1)
              }

              file := os.Args[1]
              fmt.Println("📂 Arquivo recebido:", file)

              // Verifica se o arquivo existe
              _, err := os.Stat(file)
              if os.IsNotExist(err) {
                  fmt.Println("❌ Erro: O arquivo não existe!")
                  os.Exit(1)
              }

              // Ler o arquivo
              fmt.Println("📖 Tentando ler o arquivo...")
              data, err := os.ReadFile(file)
              if err != nil {
                  fmt.Println("❌ Erro ao ler o arquivo:", err)
                  os.Exit(1)
              }

              fmt.Println("📖 Arquivo lido com sucesso!")

              // Carregar o OpenAPI Document
              fmt.Println("📦 Tentando carregar o documento OpenAPI...")
              doc, err := libopenapi.NewDocument(data)
              if err != nil {
                  fmt.Println("❌ Erro ao carregar OpenAPI:", err)
                  os.Exit(1)
              }

              fmt.Println("✅ OpenAPI carregado com sucesso!")

              // Obtém informações do OpenAPI
              fmt.Println("🔍 Obtendo informações do OpenAPI...")
              info := doc.GetSpecInfo()
              if info == nil {
                  fmt.Println("❌ Erro: Não foi possível obter informações da especificação OpenAPI")
                  os.Exit(1)
              }

              fmt.Println("✅ OpenAPI validado com sucesso! Versão:", info.Version)
          }' > validate.go

      - name: Instalar Dependências do Go
        run: |
          go mod init validator || true
          go mod tidy
          go get github.com/pb33f/libopenapi

      - name: Rodar PB33F e gerar relatório com logs detalhados
        run: |
          echo "🔍 Buscando swagger.yaml da versão do PR..."
          if [ ! -f "swagger.yaml" ]; then
            echo "❌ Arquivo swagger.yaml não encontrado na versão do PR!"
            exit 1
          fi

          echo "✅ Arquivo encontrado: swagger.yaml"
          echo "🟢 Executando validação PB33F..."

          go run validate.go swagger.yaml > pb33f_report.txt 2>&1 || {
            echo "❌ Erro ao executar PB33F. Veja detalhes abaixo:"
            cat pb33f_report.txt
            exit 1
          }

          echo "✅ PB33F finalizado com sucesso!"

      - name: Instalar Spectral para validação OpenAPI
        run: npm install -g @stoplight/spectral-cli

      - name: Rodar Spectral e gerar relatório (usando ruleset `spectral:oas`)
        run: |
          if [ ! -f "swagger.yaml" ]; then
            echo "❌ Arquivo swagger.yaml não encontrado na versão do PR!"
            exit 1
          fi

          echo "✅ Arquivo encontrado: swagger.yaml"
          spectral lint swagger.yaml --ruleset spectral:oas > spectral_report.txt || {
            echo "❌ Erro ao executar Spectral. Veja detalhes abaixo:"
            cat spectral_report.txt
            exit 1
          }

          echo "✅ Spectral finalizado com sucesso!"

      - name: Instalar Vale CLI para validação de documentação
        run: |
          curl -fsSL https://install.vale.sh | sh
          vale --version

      - name: Rodar Vale CLI e gerar relatório
        run: |
          if [ ! -f "swagger.yaml" ]; then
            echo "❌ Arquivo swagger.yaml não encontrado na versão do PR!"
            exit 1
          fi

          echo "✅ Arquivo encontrado: swagger.yaml"
          vale "swagger.yaml" > vale_report.txt

      - name: Upload de Relatórios de Validação no GitHub
        uses: actions/upload-artifact@v4
        with:
          name: validation-reports
          path: |
            pb33f_report.txt
            spectral_report.txt
            vale_report.txt
