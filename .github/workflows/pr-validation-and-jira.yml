name: Validate PR and Create Jira Task

on:
  pull_request_target:  # Executa no repositório original, permitindo acesso às secrets
    types:
      - opened
      - synchronize
      - reopened

jobs:
  validate-and-jira:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout do repositório original (para acessar secrets)
      - name: Checkout do repositório original
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.pull_request.base.repo.full_name }}  # Código base
          ref: ${{ github.event.pull_request.base.ref }}
          fetch-depth: 0

      # 2️⃣ Checkout do código atualizado do PR (fork)
      - name: Checkout do código do PR (com submódulos)
        uses: actions/checkout@v4
        with:
          repository: ${{ github.event.pull_request.head.repo.full_name }}  # Código do PR do fork
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
          submodules: true

      - name: Garantir que os Submódulos Estão Atualizados
        run: git submodule update --init --recursive

      - name: Verificar a Branch Atual (Confirma PR)
        run: git branch --show-current

      - name: Debug - Listar Arquivos na Pasta `rules`
        run: ls -R rules/

      - name: Debug - Exibir Caminho Completo do `swagger.yaml`
        run: |
          echo "Listando arquivos no diretório atual:"
          ls -lah
          echo "Procurando por swagger.yaml:"
          find $(pwd) -name "swagger.yaml"

      - name: Garantir Permissões de Leitura no `swagger.yaml`
        run: chmod +r swagger.yaml || true

      - name: Instalar Dependências do Go
        run: |
          go mod init validator || true
          go mod tidy
          go get github.com/pb33f/libopenapi

      - name: Rodar PB33F e gerar relatório usando `rules/validator.go`
        run: |
          echo "🔍 Buscando swagger.yaml..."
          if [ ! -f "swagger.yaml" ]; then
            echo "❌ Arquivo swagger.yaml não encontrado na versão do PR!"
            exit 1
          fi

          if [ ! -f "rules/validator.go" ]; then
            echo "❌ Arquivo validator.go não encontrado em rules/!"
            exit 1
          fi

          echo "✅ Arquivo encontrado: swagger.yaml"
          echo "🟢 Executando validação PB33F..."

          go run rules/validator.go swagger.yaml > pb33f_report.txt 2>&1 || true

          echo "✅ PB33F finalizado com sucesso!"

      - name: Instalar Spectral para validação OpenAPI
        run: npm install -g @stoplight/spectral-cli

      - name: Rodar Spectral e gerar relatório usando `rules/spectral_rules.yaml`
        run: |
          if [ ! -f "swagger.yaml" ]; then
            echo "❌ Arquivo swagger.yaml não encontrado na versão do PR!"
            exit 1
          fi

          if [ ! -f "rules/spectral_rules.yaml" ]; then
            echo "❌ Arquivo de regras do Spectral não encontrado!"
            exit 1
          fi

          echo "✅ Arquivo encontrado: swagger.yaml"
          spectral lint swagger.yaml --ruleset rules/spectral_rules.yaml > spectral_report.txt 2>&1 || true

          echo "✅ Spectral finalizado com sucesso! Erros e warnings foram salvos no relatório."

      - name: Instalar Vale CLI para validação de documentação
        run: |
          echo "🔽 Baixando e instalando Vale CLI..."
          VALE_VERSION=$(curl -s https://api.github.com/repos/errata-ai/vale/releases/latest | jq -r .tag_name)
          curl -L -o vale.tar.gz "https://github.com/errata-ai/vale/releases/download/$VALE_VERSION/vale_${VALE_VERSION:1}_Linux_64-bit.tar.gz"
          tar -xzf vale.tar.gz
          sudo mv vale /usr/local/bin/
          vale --version

      - name: Garantir que a pasta `rules/styles/` existe
        run: |
          if [ ! -d "rules/styles" ]; then
            echo "⚠️ Criando a pasta rules/styles/ para evitar erro no Vale CLI..."
            mkdir -p rules/styles
          fi

      - name: Criar Task no Jira
        id: create-jira-task
        env:
          JIRA_URL: ${{ secrets.JIRA_URL }}
          JIRA_USER: ${{ secrets.JIRA_USER }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
          JIRA_PROJECT: ${{ secrets.JIRA_PROJECT }}
          JIRA_ISSUE_TYPE: "Task"
        run: |
          PR_TITLE="${{ github.event.pull_request.title }}"
          PR_URL="${{ github.event.pull_request.html_url }}"
          PR_AUTHOR="${{ github.event.pull_request.user.login }}"

          DESCRIPTION=$(echo "Revisão do PR: $PR_TITLE. Link do PR: $PR_URL Criado por: $PR_AUTHOR" | jq -R .)

          ISSUE_PAYLOAD=$(jq -n \
            --arg project "$JIRA_PROJECT" \
            --arg summary "Revisão do PR: $PR_TITLE" \
            --arg description "$DESCRIPTION" \
            --arg issuetype "$JIRA_ISSUE_TYPE" \
            '{
              fields: {
                project: { key: $project },
                summary: $summary,
                description: $description,
                issuetype: { name: $issuetype }
              }
            }')

          echo "$ISSUE_PAYLOAD"
          echo "📝 Criando issue no Jira..."
          RESPONSE=$(curl -s -X POST -H "Content-Type: application/json" \
            -u "$JIRA_USER:$JIRA_API_TOKEN" \
            --data "$ISSUE_PAYLOAD" "$JIRA_URL/rest/api/2/issue/")

          ISSUE_KEY=$(echo "$RESPONSE" | jq -r '.key')

          if [[ "$ISSUE_KEY" == "null" ]]; then
            echo "❌ Erro ao criar issue no Jira!"
            echo "Resposta da API: $RESPONSE"
            exit 1
          fi

          echo "JIRA_ISSUE_KEY=$ISSUE_KEY" >> $GITHUB_ENV
          echo "✅ Tarefa Jira criada: $ISSUE_KEY"

      - name: Anexar Relatórios na Task Jira
        env:
          JIRA_URL: ${{ secrets.JIRA_URL }}
          JIRA_USER: ${{ secrets.JIRA_USER }}
          JIRA_API_TOKEN: ${{ secrets.JIRA_API_TOKEN }}
        run: |
          ISSUE_KEY="${{ env.JIRA_ISSUE_KEY }}"

          if [ -z "$ISSUE_KEY" ] || [ "$ISSUE_KEY" == "null" ]; then
            echo "❌ Nenhuma issue Jira foi criada. Pulando upload de arquivos."
            exit 1
          fi

          echo "📂 Anexando relatórios à issue: $ISSUE_KEY"
          for FILE in pb33f_report.txt spectral_report.txt vale_report.txt; do
            if [[ -f "$FILE" ]]; then
              curl -X POST -H "X-Atlassian-Token: no-check" \
                -H "Content-Type: multipart/form-data" \
                -u "$JIRA_USER:$JIRA_API_TOKEN" \
                -F "file=@$FILE" "$JIRA_URL/rest/api/2/issue/$ISSUE_KEY/attachments"
            else
              echo "⚠️ Arquivo não encontrado: $FILE"
            fi
          done
          echo "✅ Todos os relatórios foram anexados com sucesso!"
